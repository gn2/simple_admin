- page_title("Creating #{object_name}")
=render :partial => 'sidebar'

.form_holder
  -form_for( current_object, :url => objects_path ) do |f|
    =render :partial => "admin/generic/form", :locals => { :f => f}
    %p.submit
      =f.submit "Save your changes"
